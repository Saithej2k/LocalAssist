# iOS Screenshots

The PNGs in this folder are deterministic iPhone-sized screenshots generated from `Tools/Screenshots/render-screenshots.js`.

They mirror the SwiftUI surface in `LocalAssistAppUI` and are useful for README/App Store planning in this Command Line Tools environment. A full Xcode installation can replace these with simulator captures from the real app target.
